// DOM SELECTORS
let gridCard = document.querySelectorAll(".grid-card");
let cardCounter = document.querySelectorAll(".grid-card .card__likes");
let cardHeart = document.querySelectorAll(".grid-card button");

function upCounters() {
    console.log("test");
}